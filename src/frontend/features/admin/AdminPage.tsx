import { Alert, FormControlLabel, Stack, Switch, TextField, Typography } from '@mui/material';
import { useDemoMode, useSetDemoMode } from '../../hooks/useDemoMode';
import { useMidMapping, useSetMidMapping } from '../../hooks/useMidMapping';
import { useTerminalHeartbeats } from '../../hooks/useTerminalHeartbeats';
export function AdminPage(){const demoMode=useDemoMode(); const setDemoMode=useSetDemoMode(); const midMapping=useMidMapping(); const setMidMapping=useSetMidMapping(); const heartbeats=useTerminalHeartbeats(); return <Stack spacing={2}><Typography variant='h4'>Admin</Typography><Alert severity='info'>Demo mode, MID routing, and terminal monitor.</Alert><FormControlLabel control={<Switch checked={Boolean(demoMode.data?.enabled)} onChange={(_,checked)=>setDemoMode.mutate(checked)} />} label={demoMode.data?.enabled?'Demo mode enabled':'Demo mode disabled'} /><TextField label='Parts MID' value={midMapping.data?.parts ?? ''} onChange={(e)=>setMidMapping.mutate({parts:e.target.value})} /><TextField label='Service MID' value={midMapping.data?.service ?? ''} onChange={(e)=>setMidMapping.mutate({service:e.target.value})} /><TextField label='Body Shop MID' value={midMapping.data?.body_shop ?? ''} onChange={(e)=>setMidMapping.mutate({body_shop:e.target.value})} />{(heartbeats.data??[]).map((i)=><Alert key={i.terminalId} severity={i.status==='online'?'success':i.status==='offline'?'warning':'info'}>{i.terminalId} • {i.status}</Alert>)}</Stack>;}
