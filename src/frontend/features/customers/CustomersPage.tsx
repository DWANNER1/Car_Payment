import { Typography } from '@mui/material';

export function CustomersPage() {
  return (
    <div>
      <Typography variant="h4">Customers</Typography>
      <Typography color="text.secondary">Starter page for token management, customer lookup, and default payment methods.</Typography>
    </div>
  );
}
