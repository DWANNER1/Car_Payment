import { Router } from 'express';
import { terminalSaleSchema, tokenSaleSchema, finalizePaymentSchema, partialRefundSchema } from '../schemas/payment.js';
import { transactionStore } from '../services/transactionStore.js';
let seq=1;
const serviceMid='MID-SERVICE-DEMO';
export const paymentsRouter=Router();
paymentsRouter.post('/payments/terminal-sale',(req,res)=>{const parsed=terminalSaleSchema.safeParse(req.body); if(!parsed.success) return res.status(400).json({error:'invalid terminal sale payload'}); const txId=`demo-terminal-${seq++}`; transactionStore.add({gatewayTransactionId:txId,roNumber:parsed.data.roNumber,amount:parsed.data.amount,status:'accepted',departmentId:parsed.data.departmentId??'mixed'}); res.status(202).json({transactionId:txId,status:'accepted',routingMid:parsed.data.routingMid ?? serviceMid,mixedRule:'default-service-mid'});});
paymentsRouter.post('/payments/token-sale',(req,res)=>{const parsed=tokenSaleSchema.safeParse(req.body); if(!parsed.success) return res.status(400).json({error:'invalid token sale payload'}); const txId=`demo-token-${seq++}`; transactionStore.add({gatewayTransactionId:txId,roNumber:parsed.data.roNumber,amount:parsed.data.amount,status:'accepted',departmentId:parsed.data.departmentId??'mixed'}); res.status(202).json({transactionId:txId,status:'accepted',routingMid:parsed.data.routingMid ?? serviceMid,mixedRule:'default-service-mid'});});
paymentsRouter.post('/payments/finalize',(req,res)=>{const parsed=finalizePaymentSchema.safeParse(req.body); if(!parsed.success) return res.status(400).json({error:'invalid finalize payload'}); transactionStore.finalize(parsed.data.gatewayTransactionId); res.json({ok:true,dmsWriteBack:{surchargeLineItem:{code:'NON_TAX_MISC_FEE',amount:parsed.data.surchargeAmount}}});});
paymentsRouter.post('/payments/partial-refund',(req,res)=>{const parsed=partialRefundSchema.safeParse(req.body); if(!parsed.success) return res.status(400).json({error:'invalid partial refund payload'}); const m=transactionStore.partialRefund(parsed.data.gatewayTransactionId, parsed.data.refundAmount); if(!m) return res.status(404).json({error:'transaction not found'}); res.json({ok:true,refunded:parsed.data.refundAmount});});
