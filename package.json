{
  "name": "car-payment",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "src/api",
    "src/frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w src/api\" \"npm run dev -w src/frontend\"",
    "build": "npm run build -w src/api && npm run build -w src/frontend",
    "start": "npm run start -w src/api",
    "test": "npm run test -w src/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
